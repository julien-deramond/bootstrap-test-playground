# Copy this file to `.env.local` (git-ignored) to test a local Bootstrap checkout
# instead of the `v6-dev` branch installed from GitHub in node_modules.
# Relative paths resolve from this project's root.
BOOTSTRAP_PATH=../twbs/bootstrap
